export const getters = {
  started(state) {
    return state.started;
  },

  round(state) {
    return state.round;
  },

  params(state) {
    return state.params;
  },

  ended(state) {
    return state.ended;
  },
};
